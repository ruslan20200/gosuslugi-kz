[build]
  command = "pnpm install --frozen-lockfile && pnpm build"
  publish = "dist/public"

# SPA routing (wouter): serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
